import { test, expect } from '@playwright/test';

test.describe('EPAM client work navigation', () => {
  test('opens Services and verifies Client Work page', async ({ page }) => {
    // Test steps added in subsequent edits.
  });
});
